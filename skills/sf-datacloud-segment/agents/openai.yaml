interface:
  display_name: "Data 360 Segment"
  short_description: "Segments, counts, and publish proof"
  brand_color: "#127CC1"
  default_prompt: "Use $sf-datacloud-segment to create or debug a Data 360 segment with count and status proof."

policy:
  allow_implicit_invocation: true
